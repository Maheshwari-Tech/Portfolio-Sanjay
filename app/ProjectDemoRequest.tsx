"use client";
import { FormEvent, useState } from "react";
import Link from "next/link";
import { apiFetch, authHeaders, isLoggedIn } from "./apiClient";
export default function ProjectDemoRequest({projectId,projectName}:{projectId:string;projectName:string}){const [open,setOpen]=useState(false);const [status,setStatus]=useState("");const [busy,setBusy]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();if(!isLoggedIn()){window.location.href="/login";return;}setBusy(true);const form=new FormData(e.currentTarget);try{const r=await apiFetch("/submissions",{method:"POST",headers:authHeaders(),body:JSON.stringify({type:"demo",title:`Demo request — ${projectName}`,project_id:projectId,name:form.get("name"),email:form.get("email"),message:form.get("message")})});setStatus(r.ok?"Request received. Sanjay will get back to you.":"The request was not accepted. Please sign in again and retry.");}catch{setStatus("The request service is offline. Your request was not submitted.");}finally{setBusy(false)}}return <section className="demo-request"><div><p className="eyebrow">REQUEST A DEMO</p><h2>Want to see {projectName} in context?</h2><p>Share what you would like to explore.</p></div><button className="button button-dark" onClick={()=>setOpen(!open)}>{open?"Close form":"Request demo ↗"}</button>{open&&<form onSubmit={submit}><label>Name<input name="name" required/></label><label>Work email<input name="email" type="email" required/></label><label>Context<textarea name="message" required rows={4}/></label><button disabled={busy} className="button button-dark">{busy?"Submitting…":"Submit request ↗"}</button>{!isLoggedIn()&&<p>Demo requests require <Link href="/login">sign in</Link>.</p>}{status&&<p className="form-status">{status}</p>}</form>}</section>}
